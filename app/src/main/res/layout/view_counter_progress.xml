<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="@dimen/gr_size_36"
    android:layout_height="@dimen/gr_size_36">

    <ProgressBar
        android:id="@+id/viewProgress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:max="100"
        android:padding="@dimen/gr_size_1"
        android:progress="0"
        android:progressDrawable="@drawable/can_progress_ring"
        android:visibility="invisible"
        android:rotation="-90" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/countTv"
        style="@style/OrderCansAppearance"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginHorizontal="@dimen/gr_size_4"
        android:background="@drawable/bkg_underline"/>

</androidx.constraintlayout.widget.ConstraintLayout>